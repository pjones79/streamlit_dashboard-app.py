"""Minimal Streamlit page. If this runs but app.py does not, the bug is in app.py."""
import streamlit as st

st.title("Smoke test")
st.write("If you see this, Streamlit is running. Close this and use RUN_STREAMLIT.bat with app.py.")
